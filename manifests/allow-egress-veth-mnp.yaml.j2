kind: MultiNetworkPolicy
apiVersion: k8s.cni.cncf.io/v1beta1
metadata:
  namespace: {{ name_space }}
  name: allow-egress-veth-mnp
  labels:
    tft-tests: "{{ index }}"
  annotations:
    k8s.v1.cni.cncf.io/policy-for: default/ovn-stream-veth
spec:
  podSelector: {}
  egress:
    - to:
        - podSelector: {}
      ports:
        - port: {{ egress_port }}
          protocol: TCP