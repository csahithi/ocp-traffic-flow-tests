kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  namespace: {{ name_space }}
  name: allow-iperf-5201-ingress
  labels:
    tft-tests: "{{ index }}"
spec:
  podSelector: {}
  ingress:
    - from:
        - podSelector: {}
      ports:
        - port: 5201
          protocol: TCP
